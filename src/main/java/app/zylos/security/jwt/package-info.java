/**
 * JWT validation components: audience validator, JWKS-cache-refreshing
 * decoder, and supporting types. Composed by the auto-configuration package
 * into the standard Spring Security validator chain.
 */
@NullMarked
package app.zylos.security.jwt;

import org.jspecify.annotations.NullMarked;
