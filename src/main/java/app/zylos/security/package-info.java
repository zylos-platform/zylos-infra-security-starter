/**
 * Zylos Security Starter — Identity-validation stack for Zylos
 * services.
 *
 * <p>The starter provides JWT validation, JWKS caching, actor chain
 * validation, OPA authorization, MDC enrichment, and identity metrics
 * via Spring Boot autoconfiguration.
 *
 * <p>See package-level documentation in each sub-package for details
 * on a given capability.
 *
 * <p>All public types in this starter are null-marked by default
 * (JSpecify {@link org.jspecify.annotations.NullMarked}); nullable
 * parameters and return values are explicitly annotated with
 * {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package app.zylos.security;

import org.jspecify.annotations.NullMarked;
